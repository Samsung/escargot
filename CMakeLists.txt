CMAKE_MINIMUM_REQUIRED (VERSION 2.8)

PROJECT (ESCARGOT)


# CONFIGURATION
SET (CMAKE_VERBOSE_MAKEFILE true)
 
SET (HOST "linux" CACHE STRING "HOST")
#SET (ARCH "x64" CACHE STRING "ARCH")
SET (TYPE "interpreter" CACHE STRING "TYPE")
#SET (MODE "release" CACHE STRING "MODE")
SET (OUTPUT "bin" CACHE STRING "OUTPUT")

#SET (REACT_NATIVE) #TODO
#SET (LTO) #TODO
#SET (VENDORTEST) #TODO

INCLUDE (ProcessorCount)
PROCESSORCOUNT (NPROCS)

# INCLUDE CMAKE FILES
INCLUDE (build/config.cmake)
INCLUDE (build/flags.cmake)
INCLUDE (build/escargot.cmake)
INCLUDE (build/test.cmake)
